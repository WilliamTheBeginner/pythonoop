import unittest

from class_bin import Bin


class Testbin(unittest.TestCase):

	def test(self):
		example = Bin([split12])
		self.assertIn(split12, example)
		self.assertIn(split12, one)
		self.assertIn(split13, three)




if __name__ == '__main__':

    unittest.main()